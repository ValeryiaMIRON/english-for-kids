// вместо локалхост будет ссылка на хероку или другой сервер
// import { Endpoints } from './../types/types'
// type TCategory = { name: string; image: string };

// export async function getCategories(): Promise<TCategory[]> {
//     const response = await fetch(Endpoints.categories), {
//       const data = await response.json()
//       const array: TCategory[]=[]
//     };
//    for (let i =0; i < data.length; i++){
//    const cat: TCategory= {id: data[i].id, name: data[i].name}
//     array.push(cat)
//    }
//     return array;
// }
